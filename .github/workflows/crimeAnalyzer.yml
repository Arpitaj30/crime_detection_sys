name: Notify AI CI Bot

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  analyze-failure:
    name: Analyze CI Failure with AI Bot
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the failed repository
      - name: Checkout target repository
        uses: actions/checkout@v4

      # 2️⃣ Download CI logs artifact
      - name: Download CI logs artifact
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p logs
          gh run download ${{ github.event.workflow_run.id }} \
            --repo ${{ github.repository }} \
            --dir logs
          # Merge all .log files into one
          find logs -type f -name "*.log" -exec cat {} + > logs/ci_logs.txt || true

      # 3️⃣ Setup Python and jq
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      # 4️⃣ Send logs to AI Bot and get suggestion & patch
      - name: Get bot suggestion from AI server
        id: bot
        env:
          CI_AGENT: ${{ secrets.CI_AGENT }}
          INSTALLATION_ID: ${{ secrets.INSTALLATION_ID }}
        run: |
          set -e

          if [ -z "$CI_AGENT" ]; then
            echo "❌ CI_AGENT is not set"
            exit 1
          fi

          echo "Preparing logs..."
          LOGS_JSON=$(tail -n 200 logs/ci_logs.txt | jq -Rs .)

          echo "Sending logs to AI Bot server..."
          RESPONSE=$(curl --fail --show-error -s \
            -X POST "$CI_AGENT/analyze" \
            -H "Content-Type: application/json" \
            -d "{
              \"repo\": \"${{ github.repository }}\",
              \"run_id\": \"${{ github.event.workflow_run.id }}\",
              \"installation_id\": \"$INSTALLATION_ID\",
              \"logs\": $LOGS_JSON
            }"
          )

          echo "LLM Response:"
          echo "$RESPONSE" | jq

          # 1️⃣ Extract the JSON block inside triple backticks
          SUGGESTION_JSON=$(echo "$RESPONSE" | \
            grep -oP '(?s)```json\n(.*?)\n```' | \
            sed 's/```json//g' | sed 's/```//g')
          
          # 2️⃣ Parse patch safely from JSON
          PATCH=$(echo "$SUGGESTION_JSON" | jq -r '.patch? // empty')
          
          # 3️⃣ Extract readable suggestion text (remove triple backticks, preserve explanation)
          SUGGESTION_TEXT=$(echo "$RESPONSE" | sed 's/```json//g' | sed 's/```//g')

          # Write suggestion text
          echo "suggestion<<EOF" >> $GITHUB_OUTPUT
          echo "$SUGGESTION_TEXT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          # Write patch JSON
          echo "patch<<EOF" >> $GITHUB_OUTPUT
          echo "$PATCH" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT


      # - name: Comment PR with AI Bot suggestion
      #   if: ${{ github.event.workflow_run.pull_requests }}
      #   env:
      #     GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # 👈 THIS is required
      #   run: |
      #     PR_NUMBER=$(jq -r '.[0].number' <<< '${{ toJson(github.event.workflow_run.pull_requests) }}')

      #     if [ -z "$PR_NUMBER" ] || [ "$PR_NUMBER" = "null" ]; then
      #       echo "No PR found, skipping comment."
      #       exit 0
      #     fi
      #     echo "Commenting on PR #$PR_NUMBER..."
          
      #     COMMENT_BODY=$(cat << EOF
      #     🤖 **AI CI Bot Analysis**
          
      #     **Suggestion:**
      #     ${{ steps.bot.outputs.suggestion }}
          
      #     **Patch (if available):**
      #     \`\`\`json
      #     ${{ steps.bot.outputs.patch }}
      #     \`\`\`
      #     EOF
      #         )
          
              # gh pr comment "$PR_NUMBER" \
              #   --repo "${{ github.repository }}" \
              #   --body "$COMMENT_BODY"

          
      # 5️⃣ Comment PR with bot suggestion and patch
      - name: Comment PR with AI Bot suggestion
        if: ${{ github.event.workflow_run.pull_requests }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BOT_SUGGESTION=$(python -c "from bot.error_analyzer import analyze_error; print(analyze_error(open('../logs/ci_logs.txt').read()))")